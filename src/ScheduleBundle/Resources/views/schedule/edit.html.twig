{% extends 'ScheduleBundle:Schedule:layout.html.twig' %}
{% set title = 'title.schedule.default' %}

<div class="schedule_form">
    {{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form) }}
    </div>
    {{ form_end(form) }}
</div>